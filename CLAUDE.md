## Branching strategy

Branch new feature work off `dev`, not `main`. Open pull requests against
`dev` as the base branch. `main` tracks released/deployed state; `dev` is
the integration branch new work builds on.

@AGENTS.md
